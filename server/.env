# Security
SECRET_KEY="super-secret-key"
ACCESS_TOKEN_EXPIRE_MINUTES=1440
ALGORITHM=HS256


# Mongo
MONGO_URI="mongodb://127.0.0.1:27017"
MONGO_DB="fastapi_cookie_auth"


# Cookies (for local dev with React on localhost)
COOKIE_NAME=access_token
COOKIE_SECURE=false # true in production with HTTPS
COOKIE_SAMESITE=lax # needed when frontend is on a different origin
COOKIE_DOMAIN="" # e.g. .example.com (leave blank for localhost)

# Groq API Key
GROQ_API_KEY=""

# CORS — add your React app origins
CORS_ORIGINS=["http://localhost:5173","http://localhost:3000", "http://localhost:8080"]